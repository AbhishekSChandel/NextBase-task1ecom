# Firebase Configuration for Frontend
# Copy this file to .env and fill in your Firebase project credentials

# Firebase Config (for existing Firestore operations - backward compatibility)
FIREBASE_API_KEY=your-api-key-here
FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id
FIREBASE_APP_ID=your-app-id
FIREBASE_MEASUREMENT_ID=your-measurement-id

# EXPO_PUBLIC_ prefixed variables (for Firebase Auth Module)
# These are accessible via process.env in the app
EXPO_PUBLIC_FIREBASE_API_KEY=your-api-key-here
EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
EXPO_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id
EXPO_PUBLIC_FIREBASE_APP_ID=your-app-id

# Google OAuth Web Client ID (for Firebase Auth Module)
# Get from: Firebase Console → Authentication → Sign-in method → Google → Web SDK configuration
# Or: Google Cloud Console → APIs & Services → Credentials → Web client (auto created by Google Service)
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=your-web-client-id.apps.googleusercontent.com

# Google OAuth iOS Client ID (for Firebase Auth Module)
# Get from: Google Cloud Console → APIs & Services → Credentials → Create credentials → OAuth client ID → iOS
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=your-ios-client-id.apps.googleusercontent.com

# Google OAuth Android Client ID (for backward compatibility with existing config)
# Get from: Google Cloud Console → APIs & Services → Credentials → Create credentials → OAuth client ID → Android
GOOGLE_WEB_CLIENT_ID=your-web-client-id.apps.googleusercontent.com
GOOGLE_ANDROID_CLIENT_ID=your-android-client-id.apps.googleusercontent.com

# How to get Firebase values:
# 1. Go to Firebase Console: https://console.firebase.google.com/
# 2. Select your project (or create a new one)
# 3. Click the gear icon (⚙️) > Project Settings
# 4. Scroll down to "Your apps" section
# 5. If no web app exists, click "Add app" and select Web (</>) icon
# 6. Register your app with a nickname (e.g., "Fruitzy Web")
# 7. Copy the config values from the Firebase SDK snippet
# 8. Paste them here in your .env file

# How to setup Google Sign-In for Firebase Auth:
# 1. Go to Firebase Console → Authentication → Sign-in method
# 2. Enable Google Sign-In provider
# 3. Get Web Client ID from Firebase Console (or Google Cloud Console)
# 4. For iOS: Create OAuth client ID in Google Cloud Console → iOS
# 5. For Android: Register SHA-1 fingerprint in Firebase Console → Project Settings → Your Android App
# 6. Add SHA-1 to Google Cloud Console → APIs & Services → Credentials → Your OAuth 2.0 Client ID
